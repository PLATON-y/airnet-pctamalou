# Préférer : pip install -e ".[dev]"
cryptography>=42.0.0
pytest>=7.0

# Extras optionnels
# PyQt5>=5.15          # airnet[gui]
# PyAudio>=0.2.11      # airnet[voice]

# legacy/ uniquement (DEPRECATED — ne pas installer pour le chemin sécurisé)
# numpy, scapy, …
